# @codewell/is-image
Determine if an object is an image

## Installation
Not yet available
```
npm install @codewell/is-image
```

## Basic usage
```JavaScript
import isImage from '@codewell/isImage';

const img = new Image();
isImage(img); // => true
isImage({}); // => false
```

